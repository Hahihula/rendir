# Currently Unavailable

This feature is planned but not yet implemented. See `examples/README.md` for the full roadmap.

## Planned Capabilities

- Recipe discovery from .md files in folder trees
- Category from folder path + frontmatter fallback
- Tags from frontmatter + auto-extract from content
- `:::ingredients` component with amount parsing:
  ```markdown
  :::ingredients
  * 3 eggs
  * 150g flour
  * 100g sugar
  :::
  ```
- Live portion recalculation (ratio-based, no unit conversion)
- Steps as plain ordered Markdown lists
- Title fallback to first `# heading`
- Cookbook templates: index, category, recipe, archive
- Tag browsing and category pages

## Status

See `PLAN.md` — **Phase 5: Cookbook**